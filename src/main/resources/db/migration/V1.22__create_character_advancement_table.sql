CREATE TABLE `charactersheet`.`character_advancement` (
  `id_character_advancement` BIGINT NOT NULL,
  `total_level` INT NULL,
  `proficiency_bonus` INT NULL,
  `experience_points` INT NULL,
  PRIMARY KEY (`id_character_advancement`));
